package org.magic.magicaddons.render

import net.fabricmc.fabric.api.client.rendering.v1.world.WorldRenderEvents
import net.minecraft.client.MinecraftClient
import net.minecraft.client.render.RenderLayer
import net.minecraft.util.math.BlockPos
import net.minecraft.util.math.Position
import org.magic.magicaddons.Common
import org.magic.magicaddons.data.WorldPosition

object RenderManager {
    /*
    init {
        WorldRenderEvents.AFTER_ENTITIES.register { context ->

            Waypoint.renderBox(context, WorldPosition(5F,-45F,5F) , WorldPosition(10F,-35F,10F))
            Waypoint.renderThickBox(context, WorldPosition(-5F,-45F,-5F) , WorldPosition(-10F,-35F,-10F))
        }
    }

     */
}